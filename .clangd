# Point clangd at GCC so <bits/stdc++.h> and GCC's libstdc++ resolve -- clang's
# own headers have no such file, and Codeforces judges with GNU G++ anyway.
CompileFlags:
  Compiler: /usr/bin/g++
  Add: [-std=c++20, -Wall, -Wextra, -DLOCAL]
